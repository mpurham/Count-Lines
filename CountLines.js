/*
    name: Count Lines
    description: Get the line count of your text
    author: andipaetzold
    tags: count,length,size,line
*/

function main(input) {
	input.text = `${input.text.split('\n').length} lines`;
}
